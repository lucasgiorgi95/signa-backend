services:
  - type: web
    name: signa-backend
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 8000

databases:
  - name: signa-db
    databaseName: signa_marcas
    user: signa_user
    plan: free
